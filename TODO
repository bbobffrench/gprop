Memory
- Add download tracker cache management to the tile module
- Add texture cache management to the map module

Storage
- Manage the local filesystem cache (reset if it grows too large)

CPU
- Only reconstruct and present the map texture if necessary
